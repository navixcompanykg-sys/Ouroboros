# OUROBOROS — Техническое задание (единое)

> Объединяет: галактическую симуляцию, реестр звёздных систем, планетарные системы.

---

# ЧАСТЬ I: ГАЛАКТИЧЕСКАЯ СИМУЛЯЦИЯ

---

## 1. СТРУКТУРА ПРОЕКТА

```
server/
├── galaxy_gen.go        # Генератор галактики (Go). Тег //go:build ignore.
│                        # Запуск: go run galaxy_gen.go -config config.json
├── galaxy.json          # Результат генерации. Читается браузером при старте.
├── galaxy.html          # Весь фронтенд: симуляция, физика, рендер, UI, вид системы.
├── config.html          # Браузерный редактор config.json. Открывается кнопкой ⚙.
├── config.json          # Параметры симуляции. Загружается при старте galaxy.html.
├── registry.html        # Просмотр реестра звёзд. Открывается кнопкой REGISTRY.
├── star_registry.json   # Постоянный файл реестра (создаётся при рождении первой звезды).
├── main.go              # Go HTTP-сервер. Порт 8080. Все API и раздача файлов.
├── go.mod
├── serve.bat            # Быстрый запуск на Windows.
├── systems/             # Состояния планетарных систем (создаётся при первом действии игрока).
│   ├── star_42.json
│   └── ...
├── players/             # Индексы систем по игрокам.
│   └── alice.json
└── assets/              # Визуальные ассеты (зарезервировано для 2D-анимаций).
    ├── planets/         # lava.gif, rocky.gif, ice.gif, gas_giant.gif
    └── stars/           # red_dwarf.gif, yellow_dwarf.gif, blue_giant.gif
```

### Назначение файлов

**galaxy_gen.go** — генерирует `galaxy.json` по параметрам из `config.json`. Поддерживает флаг `-seed N`. Содержит захардкоженную константу `V_FLAT = 1.3963` для расчёта начальных скоростей — должна совпадать с `config.json`.

**galaxy.html** — единый фронтенд. Загружает `galaxy.json`, запускает тики симуляции, рендерит Canvas галактики. По двойному клику на звезду открывает вид планетарной системы (оверлей). Никакой сборки не требует.

**config.html** — браузерный редактор параметров. При загрузке читает дефолты из `getDefaults()`, затем накладывает значения с сервера через `deepMerge`. При сохранении делает `POST /config`.

**⚠ ПРАВИЛО СИНХРОНИЗАЦИИ ТРЁХ ФАЙЛОВ:**

| Файл | Где менять |
|---|---|
| `galaxy.html` | Блок `let CFG = {...}` и соответствующая строка в `loadConfig()` |
| `config.json` | Соответствующий ключ в нужной секции |
| `config.html` | Функция `getDefaults()` и подпись `default: ...` у поля формы |

**registry.html** — просмотр реестра звёзд. Подключается через SSE (`EventSource('/registry/stream')`). Дельта-рендеринг: рождения через `appendChild`, смерти через точечное обновление ячеек `[data-field]`. Статистика вычисляется клиентски.

**systems/star_{id}.json** — хранит только изменения, внесённые игроками в систему (добытые ресурсы, постройки, флоты). Создаётся при первом игровом действии. Если файла нет — система существует, но нетронута.

**players/{player_id}.json** — индекс: список систем, в которых у игрока есть присутствие.

### API эндпоинты (main.go)

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/` | → `galaxy.html` |
| `GET` | `/config` | Читает и возвращает `config.json` |
| `POST` | `/config` | Записывает новый `config.json` |
| `POST` | `/generate?seed=N` | Запускает `go run galaxy_gen.go`, возвращает `galaxy.json` |
| `GET` | `/registry` | Возвращает `star_registry.json` целиком |
| `GET` | `/registry/stream` | SSE-поток: `init / birth / death / reset` |
| `GET` | `/registry/stats` | Статистика реестра (O(n), один проход) |
| `POST` | `/registry/birth` | Append записи рождения звезды |
| `POST` | `/registry/death` | Patch записи по `star_id`; автоматически обрезает мёртвые до 1000 |
| `POST` | `/registry/reset` | Очищает реестр (`[]`) |
| `GET` | `/system/{id}` | Данные планетарной системы (генерация + дельта из файла) |

---

## 2. ПРОСТРАНСТВО ГАЛАКТИКИ

```
CENTER     = GALAXY_R × 1.25
FIELD_SIZE = CENTER × 2
```

| Параметр | Значение |
|---|---|
| Радиус галактики | 120 у.е. |
| CENTER | 150, 150 у.е. |
| FIELD_SIZE | 300×300 у.е. |
| Зона хаоса (r <) | 2 у.е. |
| Буфер до края поля | 20 у.е. |

Координаты непрерывные (float). Каждый объект хранит `x, y, vx, vy, r, theta`.

Пересчёт `CENTER` и `FIELD_SIZE` происходит в функции `recalcDerived()` — вызывается при старте и после загрузки конфига.

### Обратная совместимость galaxy.json

При загрузке `galaxy.json` со старым центром функция `applyGalaxy` вычисляет старый центр как среднее координат **только звёзд**. Все объекты сдвигаются к текущему `CFG.CENTER`.

---

## 3. ОБЪЕКТЫ — ТИПЫ И МАССЫ

| Объект | Масса (у.е.) |
|---|---|
| Туманность | 1–5 |
| Астероидное поле | 6–30 |
| Формация | ≥ 31 (нет верхнего предела) |
| Красный карлик | 51–300 |
| Жёлтый карлик | 301–600 |
| Синий гигант | 601–1000 |
| Нейтронная звезда | наследуется (не определяется массой) |
| Чёрная дыра | 1001+ (без верхнего предела) |

**Примечание о формации:** формация — обязательная промежуточная стадия перед любой звездой. Объект с массой ≥ 31 при **динамическом создании** всегда становится формацией, а не звездой напрямую. Звезда рождается только через `FORMATION_TICKS (6)` тиков в `checkFormationBirth`.

**Примечание о белом карлике:** образуется трансформацией на месте при остывании красного или жёлтого карлика (T ≤ 10). Из формаций не возникает. При накоплении массы ≥ 600 (WD_NOVA_MASS) взрывается.

### Стартовые массы при генерации

| Тип | Диапазон генерации |
|---|---|
| Синий гигант | 601–800 |
| Жёлтый карлик | 301–500 |
| Красный карлик | 101–200 |
| Белый карлик | 51–100 |
| Чёрная дыра | 500–1000 (конфигурируется) |
| Астероид | 6–30 |
| Туманность | 1–5 |

### Цвета объектов

| Тип | Цвет RGB | Форма |
|---|---|---|
| Синий гигант | (160, 200, 255) | круг |
| Жёлтый карлик | (255, 224, 102) | круг |
| Красный карлик | (255, 100, 68) | круг |
| Белый карлик | (210, 235, 255) | круг |
| Нейтронная звезда | (180, 255, 210) | круг с белой точкой внутри (r×0.35) |
| Чёрная дыра | (187, 68, 255) | круг с кольцом r×2.2, stroke rgba(187,68,255,0.18) |
| Формация | (255, 200, 80) | ромб |
| Туманность | (68, 255, 204) | прямоугольник |
| Астероидное поле | (136, 153, 170) | прямоугольник |

### Прозрачность и тепловая окраска

```
alpha = 0.35 + 0.65 × clamp((mass - lo) / (hi - lo), 0, 1)
```

Горячие звёзды смещаются в белый/оранжевый: `tint = floor(temp / COLLAPSE_LIMIT × 180)`.

---

## 4. ФИЗИКА

### 4.1 Плоская кривая вращения

**Единая орбитальная скорость `V_FLAT = 1.3963 у.е./тик`** для всех объектов на любом радиусе. Орбитальный период растёт линейно: `T = 2π × r / V_FLAT`.

| r (у.е.) | Период (тиков) |
|---|---|
| 5 | ~22 |
| 10 | ~45 |
| 20 | ~90 |
| 40 | ~180 |
| 60 | ~270 |
| 80 | ~360 |

### 4.2 Интегратор движения (Leapfrog)

```
[ax, ay] = computeAccelXY(o)
vx += ax × 0.5  // Half-kick
vy += ay × 0.5
x += vx         // Drift
y += vy
vx += ax × 0.5  // Second half-kick
vy += ay × 0.5
```

### 4.3 Гравитация в computeAccelXY

Debris-объекты (флаг `debris=true`) пропускаются.

**1. Центральное притяжение:**
```
cdx = CENTER - o.x
cdy = CENTER - o.y
cd = sqrt(cdx² + cdy²) + 0.001
centripetal = V_FLAT² / cd
ax += centripetal × (cdx / cd)
ay += centripetal × (cdy / cd)
```

**2. Гравитация от массивных объектов (таблица):**

`GRAVITY_TABLE[r] = K_BASE / r^POWER` для r = 1..20, K_BASE = 0.0001, POWER = 1.5. Радиус влияния: 20 у.е.

**Глобальное снижение гравитации:** `K_BASE` снижен с 0.0001 до 0.0000667 (÷1.5). Равномерно ослабляет притяжение всех массивных объектов — звёзд, ЧД, НЗ, формаций — без изменения их масс.

**Массивные объекты** (`isMassive`): ЧД, синий гигант, нейтронная звезда, жёлтый карлик (mass ≥ 500).

| r | K[r] | a от mass=500 | a от mass=700 | a от mass=1500 |
|---|---|---|---|---|
| 1 | 0.00010000 | 0.05000 | 0.07000 | 0.15000 |
| 2 | 0.00003536 | 0.01768 | 0.02475 | 0.05303 |
| 5 | 0.00000894 | 0.00447 | 0.00626 | 0.01342 |
| 10 | 0.00000316 | 0.00158 | 0.00221 | 0.00474 |
| 15 | 0.00000172 | 0.00086 | 0.00121 | 0.00258 |
| 20 | 0.00000112 | 0.00056 | 0.00079 | 0.00168 |

### 4.4 Orbital drag (стабилизация орбит)

**Тангенциальный drag** — тянет тангенциальную скорость к V_FLAT:
```
v_tang = -vx × sinT + vy × cosT
excess = v_tang - rotDir × V_FLAT
massFactor = min(2.0, mass / 300.0)
drift = -excess × ORBITAL_DRAG × massFactor
```
`ORBITAL_DRAG = 0.05`

**Радиальный drag** — гасит радиальную компоненту скорости:
```
v_rad = vx × cosT + vy × sinT
v_rad_new = v_rad × (1 − RADIAL_DAMP)
```
`RADIAL_DAMP = 0.12`

### 4.5 Флаг Debris

- `debris = true` → не получает гравитацию и orbital drag
- `debris_ticks` — счётчик, уменьшается на 1 каждый тик; при 0 флаг снимается
- Стандартное значение: `debris_ticks = 10`

### 4.6 Начальные скорости при генерации

```
vx = -rotDir × V_FLAT × sin(θ)
vy =  rotDir × V_FLAT × cos(θ)
```

Направление `rotDir` = первый бросок RNG от сида галактики.

### 4.7 Граничные условия

- `r < CHAOS_R (2 у.е.)` → объект поглощается, масса в пул возврата
- За пределами FIELD_SIZE → объект поглощается, масса в пул возврата

### 4.8 Генерация — спиральные лучи

- 40 лучей, шаг π/20 рад
- 10 колец от CHAOS_R до GALAXY_R
- Звёзд на луч по кольцам: `[1, 2, 3, 4, 5, 7, 9, 8, 6, 5]`

### 4.9 Вероятности типов звёзд по радиусу

| Радиус (у.е.) | Синий | Жёлтый | Красный | Белый |
|---|---|---|---|---|
| 0–10 | 0% | 10% | 85% | 5% |
| 10–20 | 15% | 25% | 55% | 5% |
| 20–30 | 5% | 35% | 55% | 5% |
| 30–40 | 0% | 20% | 75% | 5% |
| 40–50 | 0% | 10% | 85% | 5% |

### 4.10 Массовый дрифт к центру

```
dr = DRIFT_K × mass / r    (у.е. за тик)
```

Применяется как **прямое смещение позиции** (после leapfrog и radial drag), не через скорость. Δr за один орбитальный период не зависит от r.

| Объект | Mass | r=20 у.е. | r=50 у.е. | r=80 у.е. |
|---|---|---|---|---|
| Красный карлик | 150 | 0.47 у.е. | 1.5 у.е. | 2.4 у.е. |
| Жёлтый карлик | 500 | 1.58 у.е. | **5.0 у.е.** | 8.0 у.е. |
| Синий гигант | 700 | 2.2 у.е. | 7.0 у.е. | 11.2 у.е. |

`DRIFT_K = 2.2e-3`

---

## 5. ПОРЯДОК ШАГОВ В ОДНОМ ТИКЕ

```
0. processCenterJets    — плазменные струи из центра
1. processReturnPool    — влёт из пула возврата
2. processCoreEjection  — выбросы при дефиците синих гигантов (BG < 20)
3. leapfrogStep         — физика (гравитация, orbital drag, radial drag, дрифт)
4. checkBoundaries      — поглощение в зоне хаоса и за полем
5. applyTemperature     — изменение T звёзд; охлаждение горячих туманностей
6. applyBHRadiation     — потеря массы ЧД, нагрев соседних звёзд
7. processStarLifecycle — смерть от остывания или перегрева
8. processUnstable      — взрыв «больших событий»
9. detectAndResolve     — детектирование и разрешение столкновений
10. drainNebulae        — всасывание туманностей звёздами и ЧД
11. processFormations   — рождение звёзд из формаций
12. updateDOM + draw    — статистика и рендер
```

### Порядок внутри leapfrogStep

```
1. Half-kick:       vx += ax×0.5,  vy += ay×0.5
2. Drift:           x  += vx,      y  += vy
3. Orbital drag:    тянет v_tang → V_FLAT
4. Radial drag:     гасит v_rad на RADIAL_DAMP
5. Second half-kick: vx += ax×0.5, vy += ay×0.5
6. Массовый дрифт:  прямая коррекция позиции dr = DRIFT_K×mass/r к центру
```

---

## 6. ДЕТЕКТИРОВАНИЕ СТОЛКНОВЕНИЙ

Пространственная сетка O(n). Расстояние < **0.9 у.е.** → столкновение.

### Смена типа при поглощении

- Туманность + облако → масса ≥ 3 → **астероид**
- Астероид + астероид → 10 ед./тик передача; при массе ≥ 31 → **формация**
- Астероид + звезда → 5 ед./тик; астероид исчезает при массе = 0
- Астероид + формация → мгновенное полное поглощение
- Формация + облако → масса ≥ 1001 → **чёрная дыра**

### Счётчики столкновений

| Пара | Счётчик |
|---|---|
| Cloud × Cloud | colCC |
| Cloud × Star | colCS |
| Star × Star | colSS |
| Star × BH | colSB |
| BH × BH | colBB |
| Cloud × BH | colCB |

---

## 7. ТЕМПЕРАТУРНАЯ СИСТЕМА

### 7.1 Пороги

- `T ≥ 1000` → коллапс (взрыв / сверхновая)
- `T ≤ 10` → смерть от остывания

### 7.2 dT/тик

| Тип | Стартовая T | dT/тик | Жизнь без нагрева (тиков) |
|---|---|---|---|
| Красный карлик | 100 | −0.3 | ~300 |
| Жёлтый карлик | 250 | −1.5 | ~160 |
| Синий гигант | 150 | +1.0 | ~850 (перегрев) |
| Нейтронная звезда | 500 | −0.2 | ~2450 |
| Белый карлик | 50 | −0.1 | ~400 |

### 7.3 Температура при столкновении

**Звезда + звезда:** `T_new = (T1 × M1 + T2 × M2) / (M1 + M2)`

**Звезда + облако:**
```
Туманность:  T_new = max(0,    T_star − mass × NEB_COOLING_PER_MASS)
Астероид:    T_new = min(1000, T_star + mass × AST_TEMP_PER_MASS)
Формация:    T_new = min(1000, T_star + mass × AST_TEMP_PER_MASS)
```

### 7.4 Тепловые коэффициенты облаков

| Тип | ΔT на у.е. массы | Направление |
|---|---|---|
| Туманность | −3 | Охлаждает |
| Горячая туманность (`hot_neb`) | +50 | Нагревает; всасывается 1 ед./20 тиков |
| Астероид | +0.5 | Нагревает |
| Формация | +0.5 | Нагревает как астероид |

---

## 8. ПРАВИЛО РАСПАДА — ДВА РЕЖИМА

### Режим 1 — Дисперсия (перегрев / остывание / переполнение)

- **60% массы** → 24 осколка через `ejectSixty` (12 направлений × 2 осколка). Ближний: смещение 0.5–1 у.е., скорость 1–2 у.е./тик. Дальний (та же ось): 1.1–1.5 у.е., 2–3 у.е./тик. `debris=true, debris_ticks=5`
- **40% массы** → остаток через `makeByMass`

| Источник | Остаток 40% |
|---|---|
| Синий гигант (перегрев) | **НЗ** (всегда) |
| Жёлтый карлик (перегрев) | Формация с V_FLAT |
| Красный карлик (перегрев) | Формация с V_FLAT |
| НЗ (mass ≥ 2000) | **ЧД** (60% разлёт + 40% → ЧД) |
| Красный / жёлтый (остывание) | Трансформация в белый карлик (нет распада) |
| НЗ / белый карлик (остывание) | Формация / астероид / туманность (remnant_decay) |
| Белый карлик (mass ≥ 600) | Туманности + осколки + формация |

### Режим 2 — Слияние (столкновение двух звёзд)

Синий гигант не участвует в слиянии — применяется Режим 1.

Для всех остальных пар:
- **40% суммарной массы** → осколки
- **60% суммарной массы** → формация в центре масс, скорость V_FLAT

| Столкновение | Формация 60% | Итог |
|---|---|---|
| Красный (150) + красный (150) | 180 у.е. | Красный карлик |
| Жёлтый (400) + красный (200) | 360 у.е. | Жёлтый карлик |
| Жёлтый (500) + жёлтый (500) | 600 у.е. | **Синий гигант** |

---

## 9. ЖИЗНЕННЫЙ ЦИКЛ ЗВЁЗД

### Пороги смены типа

| Переход | Порог массы |
|---|---|
| → Красный карлик | ≥ 101 |
| → Жёлтый карлик | ≥ 301 |
| → Синий гигант | ≥ 601 |

### Смерть от остывания (T ≤ 10)

**Красный / жёлтый карлик** → трансформируется в белый карлик на месте: `star_type = WHITE, temp = 50, age = 0`.

**НЗ / белый карлик** → правило 60/40, режим `decay`:
- > 15 → формация с V_FLAT
- 6–15 → астероид
- < 6 → туманность

### Смерть от перегрева (T ≥ 1000)

Правило 60/40, режим `explode`. Остаток: `remnant_nova` для синего гиганта, `ejecta` для остальных.

### Нейтронная звезда

- Возникает при сверхновой синего гиганта (**всегда**, без чередования с ЧД)
- При столкновении с обычными звёздами (не синий гигант) — **поглощает их** (как ЧД), другая звезда гибнет
- НЗ + НЗ → **сразу ЧД** (килонова), без промежуточной формации
- НЗ + синий гигант → синий взрывается (Режим 1), НЗ гибнет
- При mass ≥ 2000 → **коллапс в ЧД**: 60% разлёт + 40% → ЧД (~800 масс стартовой)
- Оказывает гравитационное притяжение на окружение (включена в `massiveObjects`)
- Счётчики: `nsDiedCool / nsDiedHeat / nsDiedMass(→BH) / nsDiedCollision`

---

## 10. ЧЁРНЫЕ ДЫРЫ

- Поглощают любой объект полностью: `bh.mass += other.mass`
- ЧД + ЧД → слияние
- Верхнего предела массы нет

### Потеря массы ЧД

```
radiated = ceil(BH_MASS_LOSS + mass × BH_LOSS_PCT)  // 5 + 1%
bh.mass -= radiated
```

### Нагрев соседних звёзд

```
boost = BH_HEAT_MAX × (1 − d / BH_RAD_RADIUS)
```
`BH_RAD_RADIUS = 10 у.е., BH_HEAT_MAX = 0.14 T/тик`

### Рождение ЧД

ЧД больше не рождается напрямую при сверхновой синего гиганта. Пути появления ЧД:
- НЗ + НЗ → **килонова** (прямое слияние в ЧД)
- НЗ при `mass ≥ 2000` → **коллапс в ЧД** (60% разлёт + 40% → ЧД ~800 масс)
- Формация при `mass ≥ 1001` → немедленный взрыв → ЧД

---

## 11. НЕЙТРОННАЯ ЗВЕЗДА

- Стартовая T: 500, dT/тик: −0.2
- При mass ≥ 2000 → коллапс в ЧД: 60% разлёт + 40% → ЧД (~800 масс стартовой ЧД)
- Поглощает все столкнувшиеся объекты кроме синего гиганта (включена в `massiveObjects`)

---

## 12. ФОРМАЦИИ (ЗВЁЗДНЫЕ ЯСЛИ)

### Начальная скорость

| Способ | Скорость |
|---|---|
| Слияние двух звёзд (60%) | V_FLAT |
| Остаток взрыва/остывания (40%) | V_FLAT (explode) или скорость родителя (decay) |
| Осколок `ejectSixty` | Ближний: 1–2 у.е./тик; дальний: 2–3 у.е./тик |
| Смена типа облака | V_FLAT |

### Детерминированный псевдорандом (`ejectRng`)

LCG: `1664525 × s + 1013904223`. Сид: `(tick × 73856093) XOR (round(x) × 19349663) XOR (round(y) × 83492791)`.

### Вид формации (Formation View)

Двойной клик по объекту типа `FORMATION` открывает **Formation View** — оверлей, идентичный System View по управлению (ESC / ← Galaxy), но показывающий внутреннюю структуру формации.

**Содержимое:** только астероидные кольца; ни звезды, ни планет нет. В центре рендерится пульсирующее пылевое облако (оранжевое свечение 4×r, дышит с частотой ~0.8 Гц).

**Генерация колец из LCG-сида:**

```javascript
let seed = form.id;
seed = (seed * 1664525 + 1013904223) & 0x7fffffff;
const nRings = 2 + (seed % 5);   // 2–6 колец

for (let i = 0; i < nRings; i++) {
  seed = (seed * 1664525 + 1013904223) & 0x7fffffff;
  const orbit = 3 + (seed % 27);      // орбитный слот 3–29
  seed = (seed * 1664525 + 1013904223) & 0x7fffffff;
  const density = 0.3 + (seed % 70) / 100;  // 0.30–1.00
  rings.push({ orbit, density });
}
```

Кольца рисуются пунктиром `rgba(160,140,100,0.22)`, толщина масштабируется плотностью (8–20px). Повторяющиеся орбиты не фильтруются — могут совпасть (артефакт «плотного кольца»).

**Автозакрытие:** если формация в течение просмотра эволюционирует в звезду (пропадает из `objects`), `updateSysStats` обнаруживает это и закрывает оверлей.

**Разделение состояния:** `sysData` (системный вид) и `formData` (вид формации) — взаимоисключающие переменные. Все обработчики событий (wheel, mousedown, mouseup, dblclick, keydown, resize) проверяют `if (!sysData && !formData)` для блокировки галактической карты.

### Рождение звезды из формации

Через `FORMATION_TICKS (6)` тиков:
1. `mass < 51` → продлить таймер
2. `mass ≥ 51` → родить звезду
3. `mass ≥ 1001` → немедленный 60/40 распад

| Масса | Тип |
|---|---|
| 51–300 | Красный карлик |
| 301–600 | Жёлтый карлик |
| 601–1000 | Синий гигант |

---

## 13. ВОЗВРАТ МАССЫ

`targetMass` фиксируется при загрузке. Каждый тик: `deficit = targetMass - currentMass`. Дефицит компенсируется влётом объектов с периметра.

800 точек на периметре квадрата (Fisher-Yates от сида). Скорость влёта: 4 у.е./тик, чисто радиально к центру. Тип: туманность (≤ 5) или астероид (≤ 30).

---

## 14. ПЛАЗМЕННЫЕ ВЫБРОСЫ ЯДРА

### processCoreEjection

Если синих гигантов < `BG_INJECT_BELOW (20)` — активируются выбросы.

| Режим | Условие | Струй |
|---|---|---|
| Жёсткий дефицит | nBG < 10 | floor(deficit / CORE_JET_DEFICIT_STEP) |
| Мягкий / возврат | 10 ≤ nBG < 20 | 1 |

Каждая струя — 4 горячих туманности: r 2.5–5.5 у.е., скорость 1–4 у.е./тик.

### processCenterJets

При поглощении объекта зоной хаоса — в следующем тике в том же направлении вылетает плазменная струя из 4 горячих туманностей.

### Горячие туманности (`hot_neb = true`)

- Стартовая T: 400, охлаждение: 8 T/тик (~23 тика до нормы)
- При столкновении со звездой: +mass × 50 T к звезде
- При медленном всасывании: 1 ед./20 тиков, +50 T к звезде

---

## 15. КОНСТАНТЫ — СВОДНАЯ ТАБЛИЦА

| Параметр | Значение | Описание |
|---|---|---|
| V_FLAT | **1.3963 у.е./тик** | Единая орбитальная скорость |
| RADIAL_DAMP | **0.12** | Гашение радиальной скорости за тик |
| DRIFT_K | **2.2e-3** | Массовый дрифт к центру |
| GRAVITY_TABLE K_BASE | **0.0000667** | База таблицы гравитации (снижена в 1.5× для замедления поглощения) |
| GRAVITY_TABLE POWER | 1.5 | Степень спада |
| MAX_INF_R | 20 у.е. | Радиус влияния (жёсткий обрыв) |
| ORBITAL_DRAG | 0.05 | Тангенциальный стабилизирующий drag |
| Радиус столкновения | **0.9 у.е.** | MIN_DIST |
| CHAOS_R | 2 у.е. | Зона хаоса |
| ЧД потеря/тик | **5 + 1% массы** | |
| BH_RAD_RADIUS | **10 у.е.** | Радиус нагрева от ЧД |
| BH_HEAT_MAX | **0.14 T/тик** | Максимальный нагрев от ЧД |
| DT_RED | **−0.3 T/тик** | |
| DT_YELLOW | **−1.5 T/тик** | |
| DT_BLUE | **+1.0 T/тик** | |
| DT_NEUTRON | **−0.2 T/тик** | |
| DT_WHITE | **−0.1 T/тик** | |
| WD_NOVA_MASS | **600 у.е.** | Взрыв белого карлика |
| NEB_COOLING_PER_MASS | **3** | Охлаждение от туманности |
| AST_TEMP_PER_MASS | **0.5** | Нагрев от астероида (снижен в 2× для баланса) |
| Debris ticks | 10 | Длительность карантина |
| Точки влёта | 800 | На периметре |
| INFLOW_SPEED | **4.0 у.е./тик** | |
| BIG_EVENT_MASS | 601 у.е. | Порог визуального пульса |
| НЗ коллапс при mass ≥ | **2000 у.е.** | При достижении → ЧД |
| НЗ при сверхновой | **всегда** | Синий гигант всегда оставляет НЗ |
| EJECTA_DIRS | **12** | Направлений разлёта; итого 24 осколка |
| FORMATION_TICKS | 6 | Тиков до рождения звезды |
| BG_INJECT_BELOW | **20** | Порог плазменных выбросов |
| CORE_NEB_MASS | **5 у.е.** | Масса туманности в струе |
| CORE_NEB_START_TEMP | **400** | Стартовая T горячей туманности |
| CORE_NEB_HEAT_PER_MASS | **25 T/у.е.** | Нагрев звезды при поглощении (снижен в 2×) |
| CORE_NEB_DRAIN_EVERY | **20 тиков** | Интервал медленного всасывания |
| CORE_NEB_COOLING | **8 T/тик** | Охлаждение горячей туманности |

---

## 16. ВИЗУАЛИЗАЦИЯ

- Canvas адаптивный. Зум: колесо мыши (30%–2000%). Панорамирование: перетаскивание.
- Хвосты траекторий: 3 предыдущих позиции на объект.
- Unstable: пульсирующий ореол `0.4 + 0.4 × sin(time / 150)`.
- Формация: оранжевое свечение 4×r.
- Горячая туманность: цвет интерполируется к ярко-синему `(60, 160, 255)`.

### Тултип (наведение мышью)

При наведении на объект показывается тултип. Для **звёзд** дополнительно отображаются:

| Поле | Описание |
|---|---|
| `T` | Текущая температура |
| `RAD` | Уровень радиации [0–100] |
| `MA` | Метеоритная активность [0–100] |
| `Rbirth` | Радиус рождения (у.е. от центра) |
| `Rings` | Число астероидных колец (0–2) |

`RAD` и `MA` вычисляются функциями `computeRAD(star)` / `computeMA(star)` в реальном времени при каждом показе тултипа. `birth_radius` и `n_rings` хранятся прямо на объекте звезды — устанавливаются в `buildBirthRecord` при рождении.

### Двойной клик по звезде

Открывает вид планетарной системы (оверлей). Симуляция галактики не останавливается. Закрытие: `ESC` или кнопка `← Galaxy`.

---

## 16.1 ИНДИКАТОРЫ СРЕДЫ — RAD И MA

### Уровень радиации `RAD` ∈ [0, 100]

```
T_norm = clamp((temp − 10) / 990,  0, 1)   // температура звезды
D_norm = clamp(n_danger / 5,       0, 1)   // ЧД + НЗ в радиусе BH_RAD_RADIUS (10 а.е.), макс. 5
G_norm = clamp(1 − r / GALAXY_R,   0, 1)   // близость к центру галактики

RAD = round(100 × (0.50 × T_norm + 0.35 × D_norm + 0.15 × G_norm))
```

| Компонент | Вес | Источник |
|---|---|---|
| Температура | 50% | `star.temp`, диапазон [10, 1000] |
| Опасные соседи | 35% | ЧД + НЗ в радиусе 10 а.е., предел 5 объектов |
| Центр галактики | 15% | `star.r / GALAXY_R`, ближе к центру = выше |

**Предельный случай** (temp=1000, 5 соседей, центр): RAD = 100 ✓

### Метеоритная активность `MA` ∈ [0, 100]

#### Базовая часть `MA_base` ∈ [0, 50] — постоянная

```
G_norm = clamp(1 − r / GALAXY_R,   0, 1)   // близость к центру
M_norm = clamp(mass / 800,         0, 1)   // масса звезды (макс. 800 у синего гиганта)
A_norm = n_rings / 2                        // астероидные кольца (0, 0.5 или 1)

MA_base = 50 × (0.30 × G_norm + 0.30 × M_norm + 0.40 × A_norm)
```

**Предел** (масса 800 + 2 кольца + центр): MA_base = 50 ✓

#### Временный всплеск `MA_spike` — от поглощения формации

При поглощении звездой формации массой `m` (≤ 50):

```
spike_new = min(50, m)
spike_remaining = max(0, meteor_spike_0 × (5 − elapsed) / 5)   // остаток текущего всплеска
meteor_spike_0 = min(50, spike_remaining + spike_new)
meteor_spike_tick = tick
```

На тике `t` после события (t < 5):
```
MA_spike = meteor_spike_0 × (5 − (tick − meteor_spike_tick)) / 5
```

После 5 тиков: MA_spike = 0.

#### Временный всплеск `MA_ast` — от прямой бомбардировки asteroid_field

Пока asteroid_field находится в зоне столкновения (0.9 а.е.) со звездой, каждый тик обновляется:

```
ast_spike_0    = max(ast_spike_0, min(30, ast.mass × 0.8))
ast_spike_tick = tick
```

Затухание (3 тика после разрыва контакта):
```
MA_ast = ast_spike_0 × (3 − (tick − ast_spike_tick)) / 3,  если elapsed < 3
```

Отличие от `MA_spike`: держится пока астероид рядом, сбрасывается быстрее (3 тика, не 5).

#### Итог

```
MA = clamp(MA_base + MA_spike + MA_ast, 0, 100)
```

**Худший случай**: MA_base=50 + формация mass=50 + астероид mass=37 = **100** ✓

#### Поля на объекте звезды

| Поле | Когда устанавливается |
|---|---|
| `star.birth_radius` | В `buildBirthRecord` при рождении, не меняется |
| `star.n_rings` | В `buildBirthRecord` при рождении |
| `star.meteor_spike_0` | При поглощении формации в `resolveCollision` |
| `star.meteor_spike_tick` | При поглощении формации в `resolveCollision` |
| `star.ast_spike_0` | При каждом тике контакта с asteroid_field в `resolveCollision` |
| `star.ast_spike_tick` | При каждом тике контакта с asteroid_field в `resolveCollision` |

---

## 17. ПАНЕЛЬ СТАТИСТИКИ (Sidebar)

| Панель | Содержимое |
|---|---|
| Galaxy | Objects, Total mass, Rotation, Return pool, Avg star radius |
| Simulation | Status, Tick speed, Collisions total, This tick |
| Object Types | Счётчики по 9 типам |
| Deaths | Cooled (T≤10), Overheated (T≥1000) |
| Evolution | Red→Yellow, Yellow→Blue, Born from formation, Core injected |
| Collisions | CC / CS / SS / SBH / BHBH / CBH |
| Avg Temperature | По типам звёзд |
| Avg Lifespan | По типам звёзд |
| Velocity Stats | Avg \|v\|, omega, Max \|v\|, Avg \|v\| clouds |
| Neutron Stars | Born, Died (4 причины), Avg lifespan |
| Black Holes | Born total |

---

## 18. УПРАВЛЕНИЕ СИМУЛЯЦИЕЙ

| Элемент | Действие |
|---|---|
| ▶ 1s / 3s / 10s | Запуск с интервалом тика (мс) |
| ■ STOP | Пауза |
| GRID | Переключение координатной сетки |
| SEED + ⟳ GEN | Перегенерация через `POST /generate?seed=N` |
| JUMP + ▷ GO | Прыжок к тику N (fastTick блоками по 50) |
| 💾 Export | Скачать `snapshot_tick_N.json` |
| ⚙ | Открыть `config.html` |
| REGISTRY | Открыть `registry.html` |

---

## 19. ЭКСПОРТ СНАПШОТА

`snapshot_tick_N.json`: `tick`, `rotation_dir`, `target_mass`, `return_pool`, `counters`, `objects`.

---

## 20. КОНФИГУРАЦИЯ (config.json)

### Физика (`physics`)

| Параметр | Значение |
|---|---|
| v_flat | 1.3963 |
| radial_damp | 0.12 |
| orbital_drag | 0.05 |
| drift_k | 0.0022 |
| gravity_k_base | 0.0000667 |
| gravity_power | 1.5 |
| max_influence_radius | 20 |

### Генерация (`generation`)

seed: 42, num_stars: 2000, num_black_holes: 20, num_nebulae: 200, num_asteroid_fields: 800, galaxy_radius: 120, chaos_radius: 2, min_dist: 0.9. Диапазоны масс — см. §3.

### Температура (`temperature`)

collapse_limit: 1000, death_limit: 10, nebula_temp_per_mass: 3, nebula_cooling_per_mass: 3, asteroid_temp_per_mass: 0.5, wd_nova_mass: 600. Стартовые T и dT/тик — см. §7.2.

### Чёрные дыры (`black_holes`)

mass_loss_per_tick: 5, mass_loss_pct: 0.01, radiation_radius: 10, heat_max: 0.14.

### Распад 60/40 (`dispersal`)

ejecta_fraction: 0.6, formation_ticks: 6, impulse_min: 1.5, directions: 12, big_event_mass_threshold: 601, debris_ticks: 5.

### Возврат массы (`mass_return`)

nebula_return_max: 5, asteroid_return_max: 30, inflow_speed: 4.0.

### Симуляция (`simulation`)

max_jump_tick: 2000.

### Плазменный инжектор (`blue_giant_inject`)

bg_threshold: 20, jet_deficit_step: 2, nebula_mass: 5, start_temp: 400, heat_per_mass: 25, drain_every: 20, cooling_per_tick: 8.

---

## 21. ЗАПУСК

```bash
go run galaxy_gen.go -config config.json   # Генерация галактики
serve.bat                                   # Windows
go run main.go                              # → http://localhost:8080
```

**serve.bat / launch.bat — ожидание готовности сервера:**

Оба bat-файла используют PowerShell-поллер вместо фиксированной задержки. Браузер открывается только после успешного ответа сервера (опросы каждые 500 мс, до 60 секунд):

```bat
start "" powershell -NoProfile -WindowStyle Hidden -Command ^
  "$i=0; while($i -lt 120){try{Invoke-WebRequest -Uri 'http://localhost:8080' -UseBasicParsing -TimeoutSec 1 -ErrorAction Stop; break}catch{Start-Sleep -Milliseconds 500; $i++}}; Start-Process 'http://localhost:8080'"
```

---

---

# ЧАСТЬ II: РЕЕСТР ЗВЁЗДНЫХ СИСТЕМ

**Модуль:** `star_registry.json` + `registry.html`

---

## 22. НАЗНАЧЕНИЕ

Реестр — постоянный файл `star_registry.json`, ведущийся параллельно с симуляцией. Каждая звезда при рождении получает уникальную запись с GEN-кодом планетной системы. При смерти запись дополняется. Просмотр — через `registry.html`.

### Ограничение размера

Хранит **все живые звёзды** + **последние 1000 погибших** (по убыванию `death_tick`). Обрезка выполняется автоматически при каждом `POST /registry/death`.

```go
const maxDeadRecords = 1000

func trimDeadRecords(records []map[string]interface{}) []map[string]interface{} {
    var alive, dead []map[string]interface{}
    for _, rec := range records {
        if rec["death_tick"] == nil { alive = append(alive, rec) } else { dead = append(dead, rec) }
    }
    if len(dead) <= maxDeadRecords { return records }
    sort.Slice(dead, func(i, j int) bool {
        ti, _ := dead[i]["death_tick"].(float64)
        tj, _ := dead[j]["death_tick"].(float64)
        return ti > tj
    })
    return append(alive, dead[:maxDeadRecords]...)
}
```

---

## 23. СТРУКТУРА ЗАПИСИ В РЕЕСТРЕ

### 23.1 Блок рождения (фиксируется один раз)

| Поле | Тип | Описание |
|---|---|---|
| `star_id` | int | ID объекта из движка |
| `birth_tick` | int | Тик рождения |
| `birth_radius` | float | `o.r` — расстояние от центра |
| `birth_star_type` | string | `red_dwarf` / `yellow_dwarf` / `blue_giant` / `neutron_star` |
| `birth_mass` | int | Масса при рождении |
| `birth_event` | int (0–4) | Тип события рождения |
| `neighbor_gravity_mass` | int | Сумма масс ЧД+НЗ в радиусе 10 у.е. |
| `neighbor_neb_mass` | int | Сумма масс туманностей в радиусе 5 у.е. |
| `asteroid_absorbed` | int | Масса астероидов, поглощённых формацией до рождения |
| `birth_temp_env` | float | (T_соседей + T_самой_звезды) / 2 |
| `gen_code` | string(9) | GEN-код: `S1S2S3S4S5S6S7S8S9` |
| `n_planets` | int (0–12) | Расчётное число планет |
| `n_rings` | int (0–2) | Расчётное число астероидных колец |

*Белые карлики не регистрируются при рождении — образуются трансформацией существующей звезды.*

### 23.2 Блок смерти (null до смерти)

| Поле | Тип | Описание |
|---|---|---|
| `death_tick` | int\|null | Тик смерти |
| `lifespan` | int\|null | `death_tick - birth_tick` |
| `death_cause` | string\|null | `overheat` / `overcool` / `collision` / `mass_limit` |
| `collision_partner` | string\|null | Тип партнёра при столкновении |
| `death_radius` | float\|null | Радиус от центра в момент смерти |
| `mass_at_death` | int\|null | Масса в момент смерти |
| `temp_at_death` | float\|null | Температура в момент смерти |

### Пример записи

```json
{
  "star_id": 1847,
  "birth_tick": 312,
  "birth_radius": 54.3,
  "birth_star_type": "yellow_dwarf",
  "birth_mass": 420,
  "birth_event": 0,
  "neighbor_gravity_mass": 310,
  "neighbor_neb_mass": 8,
  "asteroid_absorbed": 72,
  "birth_temp_env": 285.0,
  "gen_code": "134131073",
  "n_planets": 8,
  "n_rings": 0,
  "death_tick": null,
  "lifespan": null,
  "death_cause": null,
  "collision_partner": null,
  "death_radius": null,
  "mass_at_death": null,
  "temp_at_death": null
}
```

---

## 24. GEN-КОД: 9 цифр S1–S9

GEN-код — строка из 9 цифр (0–9). Вычисляется один раз при рождении. Используется при загрузке планетной системы.

### 24.1 S1 — Гравитационное давление соседей

**Что:** сумма масс ЧД + НЗ в радиусе 10 у.е. при рождении  
**Формула:** `S1 = min(floor(neighbor_gravity_mass / 350), 9)`

| S1 | Масса соседей | Влияние |
|---|---|---|
| 0 | 0 | Норма |
| 1–3 | 1–1050 | −1 планета |
| 4–6 | 1051–2100 | −2 планеты |
| 7–9 | 2101–3500 | −3 планеты |

### 24.2 S2 — Радиус орбиты

**Формула:** `S2 = min(floor(birth_radius / 12), 9)`

| S2 | Радиус | Влияние |
|---|---|---|
| 0 | 0–12 у.е. | −1 планета |
| 1 | 12–24 у.е. | Нейтрально |
| 2–5 | 24–72 у.е. | +1 планета |
| 6–9 | 72–120 у.е. | −1 планета, +1 к кольцам |

### 24.3 S3 — Масса звезды при рождении

**Что:** `birth_mass`, предел 1000  
**Диапазон входа:** 51–1000  
**Формула:** `S3 = min(floor((birth_mass - 1) / 100), 9)`

| S3 | Масса | Тип | Влияние |
|---|---|---|---|
| 0–2 | 1–300 | Красный карлик | Нейтрально (красные карлики — стабильные хозяева планет) |
| 3–5 | 301–600 | Жёлтый карлик | +1 планета, оптимальная зона |
| 6–8 | 601–900 | Синий гигант | −2 планеты, мощный звёздный ветер |
| 9 | 901–1000 | Предельная масса | −4 планеты, 0–3 максимум |

> **Правило предела:** если формация при очередном поглощении превышает массу 1000 — звезда не рождается. Формация немедленно взрывается (supernova) без записи в реестр.

### 24.4 S4 — Астероидная насыщенность

**Что:** масса астероидных полей, поглощённых формацией за 6 тиков до рождения  
**Диапазон входа:** 0–300  
**Формула:** `S4 = min(floor(asteroid_absorbed / 30), 9)`

| S4 | Масса | Влияние |
|---|---|---|
| 0–3 | 0–120 | Нейтрально |
| 4–6 | 121–210 | +1 планета каменистого типа |
| 7–9 | 211–300 | +2 планеты, +1 к кольцам |

### 24.5 S5 — Газовая насыщенность среды

**Что:** сумма масс туманностей в радиусе 5 у.е. в момент рождения  
**Диапазон входа:** 0–40 (туманности 1–5 единиц каждая)  
**Формула:** `S5 = min(floor(neighbor_neb_mass / 4), 9)`

| S5 | Масса | Влияние |
|---|---|---|
| 0–2 | 0–8 | −1 планета, редкие газовые гиганты |
| 3–5 | 9–20 | Норма |
| 6–9 | 21–40 | +1 газовая планета |

### 24.6 S6 — Тепловой фон среды

**Что:** средняя T соседних звёзд в радиусе 5 у.е. + стартовая T самой звезды, делённая на 2

Стартовые T по типу:

| Тип | Стартовая T |
|---|---|
| Красный карлик | 100 |
| Жёлтый карлик | 300 |
| Синий гигант | 700 |
| Нейтронная звезда | 900 |

**Если соседей нет:** `T_среда = 0`, `T_итог = birth_star_T / 2`

**Формула:** `S6 = min(floor(birth_temp_env / 100), 9)`

| S6 | T_итог | Влияние |
|---|---|---|
| 0–2 | 0–200 | Холодная среда: −1 планета в дальней зоне, ледяные тела |
| 3–5 | 201–500 | Норма |
| 6–9 | 501–900 | Горячая среда: −1 планета из внешней зоны |

### 24.7 S7 — Тип события рождения

Определяется в момент `checkFormationBirth` по истории формации:

| S7 | Событие | Как определить в коде | Влияние |
|---|---|---|---|
| 0 | Формация влетела с края галактики | `birth_event_type = 'inflow'` — формация создана через `returnMass` | +1 планета, высокий угловой момент |
| 1 | Формация из столкновения звёзд | `birth_event_type = 'star_collision'` — формация создана в `handleCollision` между двумя звёздами | −1 планета, богато тяжёлыми элементами |
| 2 | Формация из столкновения астероидных масс | `birth_event_type = 'asteroid_collision'` — источник оба объекта asteroid_field | +1 каменистая планета, +1 кольцо |
| 3 | Формация из столкновения звезда + астероид/туманность | `birth_event_type = 'star_cloud'` | Норма |
| 4 | Слияние двух формаций | `birth_event_type = 'formation_merge'` — оба объекта TYPE.FORMATION | +1 планета, увеличен разброс орбит |
| 5 | Формация из взрыва белого карлика | `birth_event_type = 'wd_nova'` — белый карлик превысил `WD_NOVA_MASS` (600 у.е.) | Нейтрально |
| 6–9 | Резерв | — | — |

**Реализация:** при создании формации в `galaxy.html` добавить поле `o.birth_event_type` на объект формации. При рождении звезды это поле копируется в запись реестра и кодируется в S7.

### 24.8 S8 — Фазовый сдвиг орбит

**Формула:** `S8 = birth_tick % 10`

Используется при расчёте начального углового положения планет (см. §35). Обеспечивает уникальный фазовый сдвиг для каждой системы, рождённой в разные тики — планеты систем-одногодок не синхронизируются.

### 24.9 S9 — Контрольная сумма

**Формула:** `S9 = (S1 + S2 + S3 + S4 + S5 + S6 + S7 + S8) % 10`

---

## 25. РАСЧЁТ ЧИСЛА ПЛАНЕТ И КОЛЕЦ

```
N_planets = clamp(7 + ΔS1 + ΔS2 + ΔS3 + ΔS4 + ΔS5 + ΔS6 + ΔS7, 0, 12)
```

Среднее при нейтральных параметрах = **7**.

| Параметр | Условие | Δ планет |
|---|---|---|
| S1 | 0 | 0 |
| S1 | 1–3 | −1 |
| S1 | 4–6 | −2 |
| S1 | 7–9 | −3 |
| S2 | 0 | −1 |
| S2 | 1 | 0 |
| S2 | 2–5 | +1 |
| S2 | 6–9 | −1 |
| S3 | 0–2 | 0 |
| S3 | 3–5 | +1 |
| S3 | 6–8 | −2 |
| S3 | 9 | −4 |
| S4 | 0–3 | 0 |
| S4 | 4–6 | +1 |
| S4 | 7–9 | +2 |
| S5 | 0–2 | −1 |
| S5 | 3–5 | 0 |
| S5 | 6–9 | +1 |
| S6 | 0–2 | −1 |
| S6 | 3–5 | 0 |
| S6 | 6–9 | −1 |
| S7 | 0 | +1 |
| S7 | 1 | −1 |
| S7 | 2 | +1 |
| S7 | 3 | 0 |
| S7 | 4 | +1 |
| S7 | 5 | 0 |

### Формула числа колец

```
ring_score = (S2 >= 6 ? 1 : 0) + (S4 >= 6 ? 1 : 0) + (S7 == 2 ? 1 : 0)
N_rings = clamp(ring_score, 0, 2)
```

---

## 26. ПРИМЕРЫ GEN-КОДОВ

### 🟢 Минимум — «Выжженная зона»

*Синий гигант, центр галактики, рядом ЧД+НЗ, горячая среда*

| S1 | S2 | S3 | S4 | S5 | S6 | S7 | S8 | S9 |
|---|---|---|---|---|---|---|---|---|
| 8 | 0 | 8 | 0 | 0 | 6 | 1 | 1 | 4 |

**GEN: `808000614` → ΔN = −9 → 0 планет, 0 колец**

### 🟡 Норма — «Жёлтый карлик средней зоны»

| S1 | S2 | S3 | S4 | S5 | S6 | S7 | S8 | S9 |
|---|---|---|---|---|---|---|---|---|
| 0 | 4 | 4 | 2 | 2 | 1 | 0 | 2 | 5 |

**GEN: `044221025` → ΔN = +1 → 8 планет, 0 колец**

### 🔴 Максимум — «Богатая система»

| S1 | S2 | S3 | S4 | S5 | S6 | S7 | S8 | S9 |
|---|---|---|---|---|---|---|---|---|
| 0 | 3 | 4 | 8 | 8 | 3 | 4 | 0 | 0 |

**GEN: `034883400` → ΔN = +6 → 12 планет, 1 кольцо**

---

## 27. ИНТЕГРАЦИЯ В galaxy.html

### 27.1 Поля объекта формации

```javascript
form.birth_event_type = 'inflow';           // влёт с края
form.birth_event_type = 'star_collision';   // столкновение двух звёзд
form.birth_event_type = 'asteroid_collision'; // столкновение астероидов
form.birth_event_type = 'star_cloud';       // звезда + облако/астероид
form.birth_event_type = 'formation_merge';  // слияние двух формаций
form.asteroid_absorbed = 0; // инкрементировать при каждом поглощении
```

### 27.2 Сканирование соседей при рождении

В `checkFormationBirth`, прямо перед записью в реестр — **один проход по массиву objects**:

```javascript
function scanNeighbors(o, radius_gravity=10, radius_neb=5) {
  let grav_mass=0, neb_mass=0, temp_sum=0, temp_count=0;
  for(const nb of objects){
    if(nb.id===o.id) continue;
    const d=dist2(o.x,o.y,nb.x,nb.y);
    if(d<=radius_gravity){
      if(nb.type===TYPE.BLACK_HOLE) grav_mass+=nb.mass;
      if(nb.star_type===STAR.NEUTRON) grav_mass+=nb.mass;
    }
    if(d<=radius_neb){
      if(nb.type===TYPE.NEBULA) neb_mass+=nb.mass;
      if(nb.type===TYPE.STAR){ temp_sum+=nb.temp; temp_count++; }
    }
  }
  const star_base_temp=startTempForStarType(o.star_type);
  const env_temp=temp_count>0?temp_sum/temp_count:0;
  return { neighbor_gravity_mass:grav_mass, neighbor_neb_mass:neb_mass,
           birth_temp_env:(star_base_temp+env_temp)/2 };
}
```

Сканирование делается **один раз при рождении**. Результат сохраняется в реестр. Повторных вычислений нет.

### 27.3 Функция вычисления GEN-кода

```javascript
function computeGenCode(r) {
  const S1=Math.min(Math.floor(r.neighbor_gravity_mass/350),9);
  const S2=Math.min(Math.floor(r.birth_radius/12),9);
  const S3=Math.min(Math.floor((r.birth_mass-1)/100),9);
  const S4=Math.min(Math.floor(r.asteroid_absorbed/30),9);
  const S5=Math.min(Math.floor(r.neighbor_neb_mass/4),9);
  const S6=Math.min(Math.floor(r.birth_temp_env/100),9);
  const S7={inflow:0,star_collision:1,asteroid_collision:2,
             star_cloud:3,formation_merge:4,wd_nova:5}[r.birth_event_type]??9;
  const S8=r.birth_tick%10;
  const S9=(S1+S2+S3+S4+S5+S6+S7+S8)%10;
  return `${S1}${S2}${S3}${S4}${S5}${S6}${S7}${S8}${S9}`;
}
```

### 27.4 Расчёт числа планет

```javascript
function computePlanets(S1, S2, S3, S4, S5, S6, S7) {
  let n = 7;

  // S1: гравитационное давление соседей
  if      (S1 >= 7) n -= 3;
  else if (S1 >= 4) n -= 2;
  else if (S1 >= 1) n -= 1;

  // S2: средняя зона оптимальна (+1), крайние зоны штраф (−1); S2=1 нейтрален
  if      (S2 >= 2 && S2 <= 5) n += 1;
  else if (S2 === 0 || S2 >= 6) n -= 1;

  // S3: маломассивные звёзды больше не штрафуются (красные карлики — стабильные хозяева)
  if      (S3 === 9)           n -= 4;
  else if (S3 >= 6)            n -= 2;
  else if (S3 >= 3 && S3 <= 5) n += 1;

  // S4: только бонус за астероидное богатство; нулевые астероиды не штрафуются; бонусы накапливаются
  if (S4 >= 4) n += 1;
  if (S4 >= 7) n += 1;

  // S5: газовая насыщенность
  if      (S5 <= 2) n -= 1;
  else if (S5 >= 6) n += 1;

  // S6: тепловой фон — штрафует и холодные, и горячие среды
  if      (S6 <= 2) n -= 1;
  else if (S6 >= 6) n -= 1;

  // S7: тип события рождения; S7===5 (wd_nova) — нейтрально
  if      (S7 === 0) n += 1;
  else if (S7 === 1) n -= 1;
  else if (S7 === 2) n += 1;
  else if (S7 === 4) n += 1;

  return Math.max(0, Math.min(12, n));
}

function computeRings(S2, S4, S7) {
  let r = 0;
  if (S2 >= 6) r++;  // периферия: рассеянный диск → кольца
  if (S4 >= 6) r++;  // много астероидов → кольца
  if (S7 === 2) r++; // рождение из астероидного столкновения
  return Math.min(2, r);
}
```

### 27.5 Жизненный цикл белых карликов

Белые карлики **не проходят через формацию** — они образуются трансформацией существующей звезды и не получают записи в реестре при «рождении».

**Образование:** когда красный или жёлтый карлик остывает ниже `DEATH_LIMIT` (10), `checkStarDeath` меняет тип на месте:
```javascript
o.star_type = STAR.WHITE;
o.temp = CFG.T_WHITE;  // 50
o.age = 0;
```

**Без реклассификации:** белый карлик, поглощающий астероиды или туманности, **не превращается обратно** в красный/жёлтый — `starTypeFromMass` для `STAR.WHITE` пропускается.

**Регистрация белых карликов из `remnant_decay`:** НЗ и белые карлики при остывании могут «рассыпаться» в новый объект через `disperseObject`. Если остаток — звезда (красный/жёлтый/синий карлик), она получает `birth_event_type='remnant_decay'` и регистрируется через `registerBirth` сразу в `disperseObject`. Без этого вызова `GET /system/{id}` возвращал бы 404 (записи в реестре нет). Это объясняет, почему белые карлики, созданные этим путём, открывались двойным кликом без ошибок только после исправления.

**Взрыв (wd_nova):** если масса белого карлика достигает `WD_NOVA_MASS` (600 у.е., параметр `config.json → temperature.wd_nova_mass`), вызывается `doWhiteDwarfNova`:
- 12 туманностей разлетаются в радиусе 5 у.е. (откусывают массу первыми)
- 60% остатка → 24 осколка (астероиды, туманности, малые формации)
- 40% остатка → формация с `birth_event_type = 'wd_nova'` и орбитальной скоростью `V_FLAT`

Из этой формации по обычным правилам `checkFormationBirth` может родиться новая звезда (чаще всего красный карлик), которая уже **попадёт в реестр**.

**Счётчики:** смерти белых карликов учитываются в `CNT.deathWD`, `CNT.lifeWD`. В реестре смерть белого карлика не фиксируется (нет записи рождения).

### 27.6 Запись рождения

```javascript
async function registerBirth(star, genCode, nPlanets, nRings, neighbors) {
  const record = {
    star_id: star.id, birth_tick: tick,
    birth_radius: +star.r.toFixed(2),
    birth_star_type: star.star_type, birth_mass: star.mass,
    birth_event: {inflow:0,star_collision:1,asteroid_collision:2,
                  star_cloud:3,formation_merge:4}[star.birth_event_type]??5,
    neighbor_gravity_mass: neighbors.neighbor_gravity_mass,
    neighbor_neb_mass: neighbors.neighbor_neb_mass,
    asteroid_absorbed: star.asteroid_absorbed??0,
    birth_temp_env: +neighbors.birth_temp_env.toFixed(1),
    gen_code: genCode, n_planets: nPlanets, n_rings: nRings,
    death_tick: null, lifespan: null, death_cause: null,
    collision_partner: null, death_radius: null, mass_at_death: null, temp_at_death: null
  };
  await fetch('/registry/birth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(record)});
}
```

### 27.7 Запись смерти

```javascript
async function registerDeath(star, cause, partnerType) {
  await fetch('/registry/death',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({
      star_id: star.id, death_tick: tick,
      lifespan: tick - star.birth_tick_registered,
      death_cause: cause, collision_partner: partnerType,
      death_radius: +star.r.toFixed(2),
      mass_at_death: star.mass, temp_at_death: +star.temp.toFixed(1)
    })
  });
}
```

---

## 28. registry.html

### Концепция

- Открывается как отдельная страница (кнопка `REGISTRY` в `galaxy.html`)
- При открытии загружает `GET /registry` и `GET /registry/stats`
- Обновляется синхронно с тиком галактики через SSE (без таймеров и опроса)
- Таблица с фильтрами, сортировкой и поиском по ID

### Колонки таблицы

| Колонка | Поле | Примечание |
|---|---|---|
| ID | `star_id` | |
| Тип | `birth_star_type` | С цветной точкой |
| GEN | `gen_code` | Моноширинный шрифт |
| Планеты | `n_planets` | |
| Кольца | `n_rings` | |
| Масса (рожд.) | `birth_mass` | |
| Радиус (рожд.) | `birth_radius` | |
| Событие | `birth_event` | Текстовый лейбл |
| Тик рожд. | `birth_tick` | |
| Статус | — | 🟢 Живая / 💀 Мёртвая |
| Тик смерти | `death_tick` | `—` если живая |
| Продолжительность | `lifespan` | `—` если живая |
| Причина смерти | `death_cause` | `—` если живая |

### Фильтры

- По типу звезды (чекбоксы: red_dwarf / yellow_dwarf / blue_giant / neutron_star)
- Только живые / только мёртвые / все
- Тик рождения: от / до
- Минимальное число планет

### Синхронизация через SSE (Server-Sent Events)

Синхронизация **событийная, без таймеров и опроса**. `registry.html` устанавливает постоянное SSE-соединение с сервером.

| Событие | Когда | Содержимое |
|---|---|---|
| `init` | При подключении | Полный массив `star_registry.json` |
| `birth` | При каждом рождении звезды | JSON-запись рождения |
| `death` | При каждой смерти звезды | JSON-патч (`star_id` + поля смерти) |
| `reset` | При перегенерации галактики | `{}` |

```javascript
const evtSource = new EventSource('/registry/stream');

evtSource.addEventListener('init', e => {
  allRecords = JSON.parse(e.data);
  renderTable();
  updateStatsFromRecords();
});

evtSource.addEventListener('birth', e => {
  const rec = JSON.parse(e.data);
  allRecords.push(rec);
  if (passesFilter(rec))
    document.getElementById('registry-tbody').appendChild(buildRow(rec));
  scheduleStatsUpdate(); // дебаунс 1500ms
});

evtSource.addEventListener('death', e => {
  const patch = JSON.parse(e.data);
  const rec = allRecords.find(r => r.star_id === patch.star_id);
  if (rec) Object.assign(rec, patch);
  // точечное обновление DOM через data-field атрибуты
  const tr = document.querySelector(`#registry-tbody tr[data-id="${patch.star_id}"]`);
  if (tr) {
    ['death_tick','lifespan','death_cause'].forEach(f => {
      const cell = tr.querySelector(`[data-field="${f}"]`);
      if (cell) cell.textContent = patch[f] ?? '—';
    });
    tr.querySelector('[data-field="status"]').textContent = '💀';
    tr.classList.add('dead-flash');
  }
  scheduleStatsUpdate();
});

evtSource.addEventListener('reset', () => {
  allRecords = [];
  document.getElementById('registry-tbody').innerHTML = '';
  updateStatsFromRecords();
});
```

**Дельта-рендеринг:** ползунок прокрутки всегда стабилен. Рождения добавляются через `appendChild` в конец tbody. Смерти обновляются точечно через `tr.querySelector('[data-field="..."]')` — без пересборки таблицы.

**Статистика:** пересчитывается на клиенте из `allRecords` с дебаунсом 1500ms.

Новые строки подсвечиваются зелёным (`border-left`). Смерти — анимацией `dead-flash`.

### Статистика (GET /registry/stats)

```json
{
  "total_registered": 2847,
  "total_alive": 1203,
  "total_dead": 1644,
  "avg_planets": 6.8,
  "stars_no_planets": 87,
  "stars_10_12_planets": 34,
  "stars_1_ring": 210,
  "stars_2_rings": 58,
  "by_type": {
    "red_dwarf":    { "alive": 710, "dead": 980 },
    "yellow_dwarf": { "alive": 380, "dead": 490 },
    "blue_giant":   { "alive": 98,  "dead": 162 },
    "neutron_star": { "alive": 15,  "dead": 12  }
  },
  "by_birth_event": { "0": 420, "1": 310, "2": 180, "3": 260, "4": 90 },
  "deaths_by_cause": { "overheat": 820, "overcool": 310, "collision": 490, "mass_limit": 24 }
}
```

### Поля статистики

| Поле | Считается по | Описание |
|---|---|---|
| `total_registered` | все записи | Всего записей в реестре (живые + мёртвые) |
| `total_alive` | `death_tick == null` | Звёзд без `death_tick` |
| `total_dead` | `death_tick != null` | Звёзд с `death_tick` |
| `avg_planets` | все записи | Среднее `n_planets`, 1 знак после запятой |
| `stars_no_planets` | все записи | `n_planets == 0` |
| `stars_10_12_planets` | все записи | `n_planets >= 10` |
| `stars_1_ring` | все записи | `n_rings == 1` |
| `stars_2_rings` | все записи | `n_rings == 2` |
| `by_type` | все записи | Живые/мёртвые по каждому типу звезды |
| `by_birth_event` | все записи | Число рождений по типу события 0–4 |
| `deaths_by_cause` | мёртвые | Число смертей по причине |

### Макет панели статистики

```
┌─────────────────────────────────────────────────────────────────────┐
│  REGISTRY  ·  Тик 500              ⟳ sync: per tick  [●]  [STOP]   │
├──────────────┬──────────────┬──────────────┬────────────────────────┤
│ ВСЕГО        │ ЖИВЫХ        │ МЁРТВЫХ      │ СРЕДНЕЕ ПЛАНЕТ         │
│ 2847         │ 1203  ●      │ 1644         │ 6.8                    │
├──────────────┴──────────────┴──────────────┴────────────────────────┤
│  Без планет  87  │  10–12 планет  34  │  1 кольцо  210  │  2 кольца  58  │
├─────────────────────────────────────────────────────────────────────┤
│  Смерти: перегрев 820 · переохлаждение 310 · столкновение 490 · предел массы 24  │
└─────────────────────────────────────────────────────────────────────┘
```

### Go-реализация `GET /registry/stats`

```go
func registryStatsHandler(w http.ResponseWriter, r *http.Request) {
    data, _ := os.ReadFile("star_registry.json")
    var records []map[string]interface{}
    json.Unmarshal(data, &records)

    alive, dead := 0, 0
    planetsSum, planetsCount := 0, 0
    noP, highP, ring1, ring2 := 0, 0, 0, 0
    byType := map[string]map[string]int{}
    byEvent := map[string]int{}
    byCause := map[string]int{}

    for _, rec := range records {
        isAlive := rec["death_tick"] == nil
        if isAlive { alive++ } else { dead++ }

        np := int(rec["n_planets"].(float64))
        nr := int(rec["n_rings"].(float64))
        planetsSum += np; planetsCount++
        if np == 0 { noP++ }
        if np >= 10 { highP++ }
        if nr == 1 { ring1++ }
        if nr == 2 { ring2++ }

        st := rec["birth_star_type"].(string)
        if byType[st] == nil { byType[st] = map[string]int{} }
        if isAlive { byType[st]["alive"]++ } else { byType[st]["dead"]++ }

        ev := fmt.Sprintf("%v", rec["birth_event"])
        byEvent[ev]++

        if !isAlive {
            cause := fmt.Sprintf("%v", rec["death_cause"])
            byCause[cause]++
        }
    }

    avg := 0.0
    if planetsCount > 0 { avg = math.Round(float64(planetsSum)/float64(planetsCount)*10) / 10 }

    stats := map[string]interface{}{
        "total_registered":    len(records),
        "total_alive":         alive,
        "total_dead":          dead,
        "avg_planets":         avg,
        "stars_no_planets":    noP,
        "stars_10_12_planets": highP,
        "stars_1_ring":        ring1,
        "stars_2_rings":       ring2,
        "by_type":             byType,
        "by_birth_event":      byEvent,
        "deaths_by_cause":     byCause,
    }
    w.Header().Set("Content-Type", "application/json")
    json.NewEncoder(w).Encode(stats)
}
```

---

## 29. ОБРАБОТКА ПЕРЕГЕНЕРАЦИИ ГАЛАКТИКИ

При нажатии кнопки `⟳ GEN` в `galaxy.html`:
1. `POST /registry/reset` — очищает `star_registry.json` (пишет пустой массив `[]`)
2. Генерация идёт как обычно
3. При начальной генерации все звёзды `galaxy.json` регистрируются через `registerBirth` за один проход (тик = 0, `birth_event = 0` для всех начальных звёзд)

---

## 30. ПРОИЗВОДИТЕЛЬНОСТЬ

- Запросы к `/registry/birth` и `/registry/death` — асинхронные (`await fetch`), не блокируют тик симуляции
- При быстром прокручивании (`Jump to Tick`) — запросы батчируются: отправка раз в 50 тиков или в конце прыжка
- `star_registry.json` хранится на сервере; `registry.html` никогда не пишет напрямую
- `GET /registry/stats` — лёгкий запрос, один O(n) проход. При реестре 10 000+ записей рассмотреть кеш, инвалидируемый при каждом `POST /registry/*`

---

## 31. ПРАВИЛО ПРЕДЕЛА МАССЫ > 1000

Если формация при поглощении превышает массу 1000 — немедленный взрыв без рождения звезды. Запись в реестр **не создаётся**. В счётчик добавить `CNT.massLimitExplosions++` для статистики.

---

---

# ЧАСТЬ III: ПЛАНЕТАРНЫЕ СИСТЕМЫ

**Модуль:** `server/systems/` + API `/system/{id}`

---

## 32. ПРИНЦИП ХРАНЕНИЯ: ТОЛЬКО ДЕЛЬТА

| Что | Хранение | Почему |
|---|---|---|
| Число планет, колец | `star_registry.json` (уже есть) | Из gen_code при рождении |
| Орбиты, тип, ресурсы планеты | **не хранится** | Детерминированно: `star_id` + `gen_code` |
| Позиция планеты | **не хранится** | Детерминированно: сервер-тайм |
| Добыча ресурсов | `systems/star_{id}.json` | Меняется игроком |
| Постройки на планетах | `systems/star_{id}.json` | Создаётся игроком |
| Орбитальные станции планет | `systems/star_{id}.json` | Создаётся игроком |
| Станции у звезды | `systems/star_{id}.json` | Создаётся игроком |
| Постройки в кольцах | `systems/star_{id}.json` | Создаётся игроком |
| Флоты в системе | `systems/star_{id}.json` | Движутся, меняют маршрут |
| Какие системы у игрока | `players/{player_id}.json` | Индекс для обзора империи |

Файл `systems/star_{id}.json` не создаётся до первого игрового действия.

---

## 33. КООРДИНАТНАЯ СИСТЕМА

Сетка `12 секторов × 36 орбит = 432 позиции`. Орбитные слоты нумеруются **0–35**.

| Орбиты сетки | Зона | Объекты |
|---|---|---|
| 0 | Зона звезды | Станции у звезды |
| 1–35 | Планетно-кольцевая зона | Планеты (0–12 штук) + астероидные кольца |
| >35 | Дальний космос | Транзит флотов, дальние станции |

**Астероидные кольца** занимают орбитные слоты из диапазона 0–35, выбранные алгоритмом нахождения зазоров (см. §34.1b). Фиксированные позиции не используются.

---

## 34. ДЕТЕРМИНИРОВАННАЯ ГЕНЕРАЦИЯ СИСТЕМЫ

**Входные данные:** `star_id`, `gen_code` из реестра.

### 34.1 Назначение орбит планетам (Go, Fisher-Yates LCG)

36 слотов (0–35). Жадный выбор: **минимум 2 свободные орбиты** между любыми двумя планетами (зазор ≥ 3 слота). При 36 слотах это позволяет разместить до 12 планет (0, 3, 6, …, 33).

```go
func assignPlanetOrbits(nPlanets int, seed int64) []int {
    slots := make([]int, 36)
    for i := range slots { slots[i] = i }
    // Fisher-Yates через LCG
    for i := 35; i > 0; i-- {
        seed = (seed*1664525 + 1013904223) & 0x7fffffff
        j := int(seed % int64(i+1))
        slots[i], slots[j] = slots[j], slots[i]
    }
    taken := make(map[int]bool)
    result := make([]int, 0, nPlanets)
    for _, s := range slots {
        if len(result) >= nPlanets { break }
        // Зазор ≥ 3: проверяем оба ближайших соседа с каждой стороны
        if !taken[s-2] && !taken[s-1] && !taken[s+1] && !taken[s+2] {
            taken[s] = true
            result = append(result, s)
        }
    }
    sort.Ints(result)
    return result
}
// seed = star_id * 1000003 + genCodeToInt(gen_code)
```

**Гарантия зазора:** минимальное расстояние между кольцами орбит ≥ 3 × шаг = 0.036 × size  
→ при size=1080px: 38.9px > 2 × 14px (max диаметр планеты) — перекрытия нет.

### 34.1b Назначение орбит астероидным кольцам (алгоритм зазоров)

Кольца размещаются **между** планетными орбитами, в серединах самых больших зазоров. Фиксированные позиции не используются.

```go
type gapInfo struct{ mid, size int }

// После сортировки планетных орбит — найти зазоры
gaps := []gapInfo{}
prev := -1
for _, p := range sortedPlanetOrbits {
    if prev >= 0 && p-prev >= 3 {
        gaps = append(gaps, gapInfo{mid: (prev+p)/2, size: p-prev})
    }
    prev = p
}
// Добавить зазоры у краёв (до первой и после последней планеты)
if len(sortedPlanetOrbits) > 0 {
    first := sortedPlanetOrbits[0]
    last  := sortedPlanetOrbits[len(sortedPlanetOrbits)-1]
    if first >= 3 { gaps = append(gaps, gapInfo{mid: first/2, size: first}) }
    if 35-last >= 3 { gaps = append(gaps, gapInfo{mid: (last+35)/2, size: 35-last}) }
}
sort.Slice(gaps, func(i, j int) bool { return gaps[i].size > gaps[j].size })

// Выбрать кольцо из топ-3 зазоров через LCG (детерминировано)
ringSeed := seed*31337 + 1
for i := 0; i < nRings && len(gaps) > 0; i++ {
    ringSeed = (ringSeed*1664525 + 1013904223) & 0x7fffffff
    topN := int64(3); if topN > int64(len(gaps)) { topN = int64(len(gaps)) }
    idx := int(ringSeed % topN)
    pos := gaps[idx].mid
    for usedOrbits[pos] { pos++ }   // сдвиг если слот занят
    usedOrbits[pos] = true
    gaps = append(gaps[:idx], gaps[idx+1:]...)
    rings = append(rings, RingInfo{RingIndex: i+1, OrbitGrid: pos})
}
```

**Следствие:** кольца всегда видны среди планет, а не только на крайних орбитах. Расположение детерминировано от `star_id`.

### 34.2 Тип планеты по зоне орбиты

36 слотов делятся на 4 зоны по 9:

| Орбитные слоты | Зона | Тип |
|---|---|---|
| 0–8  | Горячая | Лавовая |
| 9–17 | Тёплая | Каменистая |
| 18–26 | Умеренная | Ледяная |
| 27–35 | Холодная | Газовый гигант |

### 34.4 Детерминированные физические параметры планет

Шесть параметров на каждую планету, диапазон **[1, 100]**, вычисляются один раз при открытии системы и кешируются в `p._params`. Никакого `Math.random()` — только формулы из начальных данных.

#### Нормализованные входные переменные

| Переменная | Формула | Источник |
|---|---|---|
| `orbit_norm` | `orbit_index / 35` | слот орбиты |
| `M_norm` | `min(1, birth_mass / 800)` | масса звезды при рождении |
| `R_norm` | `min(1, birth_radius / 120)` | расстояние от центра галактики при рождении |
| `T_norm` | `star.temp / 500` (живая T) или `start_temp / 500` при отсутствии объекта | Динамическая: отражает текущую T звезды |
| `N_norm` | `min(1, n_planets / 12)` | число планет в системе |
| `sv(k)` | `parseInt(gen_code[(orbit_index + k) % 9]) / 9` | псевдорандом из gen_code |

#### Базовые значения по типу планеты

|  | GRAVITY | RADIUS | MASS | CORE MASS | WATER | CORE TEMP |
|---|---|---|---|---|---|---|
| Lava | 72 | 28 | 40 | 80 | 3 | 88 |
| Rocky | 55 | 40 | 38 | 58 | 22 | 55 |
| Ice | 38 | 52 | 32 | 28 | 65 | 22 |
| Gas giant | 62 | 73 | 72 | 12 | 38 | 32 |

#### Формулы (все зажаты в [1, 100])

```
GRAVITY    = base − 32·orbit_norm + 18·M_norm − 8·N_norm                + 5·sv(0) − 2.5
RADIUS     = base + 15·orbit_norm + 10·M_norm − 5·N_norm                + 4·sv(1) − 2
PLANET_MASS= base              + 20·M_norm − 12·N_norm                + 6·sv(2) − 3
CORE_MASS  = base − 8·orbit_norm  + 12·M_norm                           + 6·sv(3) − 3
WATER      = base + 25·orbit_norm − 15·T_norm − 10·R_norm              + 8·sv(4) − 4
CORE_TEMP  = base − 22·orbit_norm + 45·T_norm + 12·M_norm − 8·R_norm   + 6·sv(5) − 3
```

**Диапазон CORE_TEMP:** [1, 200]. Значение > 100 запускает каскадную трансформацию (см. §34.5).

#### Визуальный радиус планеты

Вычисляется из параметра RADIUS:
```javascript
const pr = p._params ? (4 + (p._params.radius / 100) * 10) : pv.r;
// Диапазон: [4, 14] px; oreол = max(pr × 2.2, 10)
```
Типичные значения:
| Тип | RADIUS | px |
|---|---|---|
| Lava (inner) | ~28–38 | 6.8–7.8 |
| Rocky | ~42–52 | 8.2–9.2 |
| Ice | ~55–65 | 9.5–10.5 |
| Gas giant | ~85–95 | 12.5–13.5 |

#### Панель параметров планеты (`#sys-planet-info`)

При наведении мыши на планету в system view появляется панель (нижний правый угол). Скрывается при уходе курсора или закрытии вида системы. Курсор меняется на `pointer` при попадании в зону свечения планеты.

| Бар | Описание | Цвет |
|---|---|---|
| GRAVITY | Поверхностная гравитация | Blue→Orange (≥40%) →Red (≥70%) |
| RADIUS | Планетарный радиус | `#4499aa` |
| MASS | Полная масса планеты | `#558844` |
| CORE MASS | Размер твёрдого ядра | Orange (≥70%) |
| WATER | Содержание воды/льда | Cyan (≥60%) |
| CORE TEMP | Температура ядра | Blue→Orange (≥40%)→Red (≥70%) |

### 34.5 Каскадная трансформация планет (CORE_TEMP > 100)

Если `coreTemp` превышает 100 (что возможно при высокой T звезды), планета трансформируется в следующий тип или уничтожается. Трансформация **не меняет базовый тип** в данных — `effectivePlanetType` применяется при каждом рендере и в интерфейсе.

#### Таблица трансформаций

| Базовый тип | Эффективный тип при coreTemp > 100 |
|---|---|
| `ice` | `rocky` |
| `rocky` | `lava` |
| `lava` | `destroyed` |
| `gas_giant` | `rocky` |

```javascript
const PLANET_TRANSFORM = { ice: 'rocky', rocky: 'lava', lava: null, gas_giant: 'rocky' };

function effectivePlanetType(base, coreTemp) {
  if (coreTemp <= 100) return base;
  return base in PLANET_TRANSFORM ? PLANET_TRANSFORM[base] : base;
  // null → 'destroyed'
}
```

**Уничтоженная планета** (`lava` при coreTemp > 100) — `effType === null`:
- На орбите рисуется пунктирное красное кольцо (orbit ring)
- Вместо тела планеты — медленно рассеивающееся дебрисное облако
- Панель параметров показывает «LAVA → DESTROYED»

**Трансформированная планета** — оранжевый контур вокруг тела.

**Панель параметров при трансформации:**
- Строка: `ICE → ROCKY`, `ROCKY → LAVA`, `GAS GIANT → ROCKY`
- Полоса CORE TEMP отображает реальное значение (может превышать 100); цвет `#ff2200` при переполнении
- Ширина полосы зажата в 100% визуально

**Направление:** трансформация **одностороняя** — планета никогда не остывает обратно (базовый тип зафиксирован при генерации). Газовые гиганты не образуются в процессе жизни звезды из других типов — только при начальной генерации системы.

**Обновление параметров:** `_params` пересчитывается каждые 8 кадров в `updateSysStats`, поэтому трансформация отражается в реальном времени при просмотре системы.

### 34.3 Базовые ресурсы по типу и gen_code (S3–S6)

| Тип | Гарантированные | Возможные редкие |
|---|---|---|
| Лавовая | Силикат, Металлическое железо | Тугоплавкие (S3 ≥ 6) |
| Каменистая | Силикат, Металлическое железо | Платиноиды (S4 ≥ 7), Биогенные (S6 ≤ 2) |
| Ледяная | Водяной лёд, Дейтерий | Инертные газы (S5 ≥ 6) |
| Газовый гигант | Водород, Гелий-3 | Изотопные газы (S5 ≥ 8) |

---

## 35. ПОЗИЦИИ ПЛАНЕТ — ФОРМУЛА

**Требование:** отсчёт от серверного времени (Unix timestamp), не от рождения звезды. Период кратен суточному циклу.

```javascript
const DAY = 86400; // секунд

const ORBIT_PERIODS = [
  7200,    // орбита 0:  2ч   — 12 оборотов/сутки
  10800,   // орбита 1:  3ч   — 8 оборотов/сутки
  14400,   // орбита 2:  4ч   — 6 оборотов/сутки
  21600,   // орбита 3:  6ч   — 4 оборота/сутки
  28800,   // орбита 4:  8ч   — 3 оборота/сутки
  43200,   // орбита 5:  12ч  — 2 оборота/сутки
  86400,   // орбита 6:  1 сутки
  172800,  // орбита 7:  2 суток
  259200,  // орбита 8:  3 суток
  345600,  // орбита 9:  4 суток
  518400,  // орбита 10: 6 суток
  1036800  // орбита 11: 12 суток
];

function planetAngleDeg(orbitIndex, genCode, starId, serverTimeSec) {
  const S8 = parseInt(genCode[7]) || 0;
  const phase = (S8 * 36 + orbitIndex * 30 + (starId * 7) % 360) % 360;
  const period = ORBIT_PERIODS[orbitIndex];
  return (phase + (serverTimeSec % period) / period * 360) % 360;
}
```

*Примечание: исходная формула с `birth_tick` (упомянутая в §24.8) устарела. В этом модуле используется только `serverTimeSec` — Unix-время, не привязанное к рождению звезды.*

---

## 36. СХЕМА ФАЙЛА `server/systems/star_{id}.json`

Ключи: `p1`–`p12` для планет, `r1`–`r2` для колец. Хранит только изменения от дефолта — пустые секции опускаются.

```json
{
  "star_id": 42,

  "resource_depletion": {
    "p3": { "iron": 120, "silicon": 50 },
    "r1": { "iron": 800 }
  },

  "star_stations": [
    {
      "id": "ss_001", "player_id": "alice",
      "type": "solar_collector",
      "orbit_slot": 1, "sector": 6, "level": 2, "built_at": 1748000000
    }
  ],

  "planet_buildings": {
    "p3": [
      {
        "id": "pb_001", "player_id": "alice",
        "type": "mine", "resource": "iron",
        "level": 2, "built_at": 1748000000
      }
    ]
  },

  "planet_stations": {
    "p3": [
      {
        "id": "ps_001", "player_id": "alice",
        "type": "shipyard", "level": 1, "built_at": 1748010000
      }
    ]
  },

  "ring_buildings": {
    "r1": [
      {
        "id": "rb_001", "player_id": "bob",
        "type": "mining_platform",
        "sector": 7, "level": 1, "built_at": 1748005000
      }
    ]
  },

  "fleets": [
    {
      "fleet_id": "fl_001", "player_id": "alice",
      "location": { "type": "orbit", "orbit": 3, "sector": 5 },
      "destination": { "type": "planet_orbit", "planet": "p5", "sector": 3 },
      "departure_time": 1748100000, "arrival_time": 1748106000,
      "mode": "sublight"
    }
  ]
}
```

---

## 37. СХЕМА ОБЪЕКТОВ

### 37.1 Станция у звезды (`star_stations[]`)

| Поле | Тип | Описание |
|---|---|---|
| `id` | string | Уникальный ID |
| `player_id` | string | Владелец |
| `type` | string | `solar_collector`, `relay`, `orbital_dock`, `defense_platform` |
| `orbit_slot` | int (1) | Зона звезды |
| `sector` | int (1–12) | Сектор размещения |
| `level` | int | Уровень |
| `built_at` | int | Unix timestamp |

### 37.2 Постройка на планете (`planet_buildings.p{i}[]`)

| Поле | Тип | Описание |
|---|---|---|
| `id` | string | Уникальный ID |
| `player_id` | string | Владелец |
| `type` | string | `mine`, `farm`, `factory`, `lab`, `colony` |
| `resource` | string\|null | Целевой ресурс (для `mine`) |
| `level` | int | Уровень |
| `built_at` | int | Unix timestamp |

### 37.3 Орбитальная станция планеты (`planet_stations.p{i}[]`)

| Поле | Тип | Описание |
|---|---|---|
| `id` | string | Уникальный ID |
| `player_id` | string | Владелец |
| `type` | string | `shipyard`, `defense`, `relay`, `refinery` |
| `level` | int | Уровень |
| `built_at` | int | Unix timestamp |

### 37.4 Постройка в кольце (`ring_buildings.r{i}[]`)

| Поле | Тип | Описание |
|---|---|---|
| `id` | string | Уникальный ID |
| `player_id` | string | Владелец |
| `type` | string | `mining_platform`, `defense_post` |
| `sector` | int (1–12) | Сектор в кольце |
| `level` | int | Уровень |
| `built_at` | int | Unix timestamp |

### 37.5 Флот в системе (`fleets[]`)

| Поле | Тип | Описание |
|---|---|---|
| `fleet_id` | string | Уникальный ID |
| `player_id` | string | Владелец |
| `location` | object | `{type, orbit, sector}` или `{type, planet, sector}` |
| `destination` | object\|null | Цель (null = стоит) |
| `departure_time` | int | Unix timestamp отправления |
| `arrival_time` | int | Unix timestamp прибытия |
| `mode` | string | `sublight`, `hyper` |

**Типы локации:** `"orbit"` — по сетке `{orbit:1–36, sector:1–12}`, `"planet_orbit"` — у планеты `{planet:"p3", sector}`, `"ring"` — в кольце `{ring:"r1", sector}`, `"star"` — у звезды.

---

## 38. ИНДЕКС ИГРОКА `server/players/{player_id}.json`

```json
{
  "player_id": "alice",
  "systems": [42, 156, 891],
  "updated_at": 1748100000
}
```

Обновляется при появлении или уходе игрока из системы.

---

## 39. ВИД ИЗНУТРИ: SYSTEM VIEW

### 39.1 Вход в систему

**Триггер:** двойной клик по объекту типа `star` или `formation` в `galaxy.html`.

- `star` → открывает **System View** (звезда + планеты + кольца)
- `formation` → открывает **Formation View** (только астероидные кольца; см. §12)

Симуляция галактики **не останавливается**. Поверх открывается полноэкранный оверлей.

**Выход:** `Escape` или кнопка `← Galaxy`.

### 39.2 Макет оверлея

```
┌──────────────────────────────────────────────────────────────┐
│ ← Galaxy   ★ Star #42 · yellow dwarf · mass 420             │
│            GEN: 044221025 · Planets: 8 · Rings: 0           │
├──────────────────────────────────────────────────────────────┤
│                   [ Canvas системы ]                         │
│         звезда в центре, планеты вращаются онлайн           │
└──────────────────────────────────────────────────────────────┘
```

### 39.3 Рендер canvas

**Слои:**
1. Фон (`#06080f`)
2. Орбитальные кольца — `rgba(255,255,255,0.06)`
3. Астероидные кольца — пунктир `rgba(160,140,100,0.22)`, толщина 8px
4. Звезда — свечение + ядро
5. Планеты — круги с гало
6. Подписи (планируется)

**Координаты:**
```javascript
function sysOrbitRadius(orbitIndex, size) {
  // Внутренняя орбита: 6% size, внешняя: 50% size. 36 слотов (0–35).
  return size * (0.06 + 0.44 * orbitIndex / 35);
}
// cx, cy — центр canvas; size = min(W, H)
// Шаг между слотами: 0.44/35 × size ≈ 0.01257 × size
// Минимальный зазор (gap=3): ≈ 0.0377 × size = 40.7px при 1080px
```

### 39.4 Визуальные примитивы планет (текущая реализация)

**Звёзды** — фиксированный радиус:

| Тип | Цвет | Радиус | Будущий ассет |
|---|---|---|---|
| Красный карлик | `#ff5533` | 18px | `assets/stars/red_dwarf.gif` |
| Жёлтый карлик | `#ffcc22` | 22px | `assets/stars/yellow_dwarf.gif` |
| Синий гигант | `#55aaff` | 30px | `assets/stars/blue_giant.gif` |
| Белый карлик | `#ddeeff` | 13px | — |
| Нейтронная звезда | `#aaffcc` | 10px | — |

**Планеты** — радиус динамический, вычисляется из параметра RADIUS (§34.4):
```
pr = 4 + (p._params.radius / 100) × 10    // [4, 14] px
glowR = max(pr × 2.2, 10)                 // ореол, минимум 10px
```

| Тип | Цвет | Типичный радиус | Будущий ассет |
|---|---|---|---|
| Лавовая | `#dd4411` | 6.8–7.8px | `assets/planets/lava.gif` |
| Каменистая | `#7a8870` | 8.2–9.2px | `assets/planets/rocky.gif` |
| Ледяная | `#99bbdd` | 9.5–10.5px | `assets/planets/ice.gif` |
| Газовый гигант | `#ddaa55` | 12.5–13.5px | `assets/planets/gas_giant.gif` |

### 39.5 Fallback: примитив → анимация

```javascript
function drawPlanet(ctx, x, y, planet) {
  const asset = loadedAssets[planet.visual_type];
  if (asset) {
    ctx.drawImage(asset, x - asset.width/2, y - asset.height/2);
  } else {
    drawPlanetPrimitive(ctx, x, y, planet.visual_type);
  }
}
```

### 39.6 Панель среды (Environment Panel)

Боковая панель системного вида показывает три живых индикатора, обновляемых каждые 8 кадров (`updateSysStats`):

| Бар | ID | Диапазон | Цвет |
|---|---|---|---|
| TEMP | `ss-temp-bar` | 0–1000 | Градиент от синего к красному |
| RADIATION | `ss-rad-bar` | 0–100 | Синий → оранжевый (≥40%) → красный (≥70%) |
| METEOR | `ss-met-bar` | 0–100 | Коричневый → тёмно-оранжевый (≥40%) → оранжевый (≥70%) |

`RADIATION` и `METEOR` вычисляются через `computeRAD(star)` / `computeMA(star)` — те же функции, что и в тултипе галактической карты. Источник данных — текущее состояние объекта звезды в `objects[]`.

### 39.7 Синхронизация времени с галактикой

**Принцип:** `sysSimTime` не накапливается от реального времени независимо. Он вычисляется из галактического `tick` с плавной интерполяцией между тиками.

```javascript
// Глобальные переменные
let tickStartReal = 0;   // performance.now() в начале текущего тика
let simIntervalMs = 1000; // текущий интервал тика (1000 / 3000 / 10000 мс)

// В runTick():
tickStartReal = performance.now();

// В startSim(ms):
simIntervalMs = ms;
tickStartReal = performance.now();

// В renderSysLoop():
if (sysSimRunning) {
  if (simTimer !== null) {
    // Галактика идёт: интерполяция между тиками (60 fps плавно)
    const frac = Math.min(1, (performance.now() - tickStartReal) / simIntervalMs);
    sysSimTime = (tick + frac) * sysSimSpeed;
  } else {
    // Галактика на паузе: планеты замирают в позиции текущего тика
    sysSimTime = tick * sysSimSpeed;
  }
}
// sysSimRunning = false (кнопка STOP): sysSimTime не меняется
```

**Следствия:**

| Состояние галактики | Поведение планет |
|---|---|
| Запущена (1s/тик) | Плавно вращаются, скорость определяется `sysSimSpeed` |
| Запущена (10s/тик) | Вращаются медленнее (пропорционально) |
| Пауза | Замирают в позиции текущего тика |
| Кнопка STOP (системный вид) | Заморожены на любом моменте |

**Кнопки скорости системного вида** (`sysSimSpeed`):

| Кнопка | `sysSimSpeed` | Значение |
|---|---|---|
| MIN/S | 60 | 60 сек планетного времени / тик |
| HR/S | 3600 | 3600 сек / тик (по умолчанию) |
| DAY/S | 86400 | 86400 сек / тик |
| STOP | 0 | `sysSimRunning = false` |

При открытии системы (`openSystemView`): `sysSimTime = tick × sysSimSpeed` — планеты сразу показываются в позиции, соответствующей текущему тику.

### 39.8 Анимационный цикл (текущая реализация)

```javascript
function renderSysLoop() {
  if (!sysData) return;
  if (sysSimRunning) {
    if (simTimer !== null) {
      const frac = Math.min(1, (performance.now() - tickStartReal) / simIntervalMs);
      sysSimTime = (tick + frac) * sysSimSpeed;
    } else {
      sysSimTime = tick * sysSimSpeed;
    }
  }
  drawSystem(sysSimTime);
  if (++_sysStatFrame % 8 === 0) updateSysStats();
  sysRafId = requestAnimationFrame(renderSysLoop);
}
```

---

## 40. ВИЗУАЛЬНЫЕ АССЕТЫ

**Формат:** GIF или WebP-анимация  
**Размер:** 64×64 px (планеты), 96×96 px (звёзды), прозрачный фон

Сервер раздаёт через статический маршрут `/assets/*`:
```go
"/assets/planets/lava.gif":      "image/gif",
"/assets/planets/rocky.gif":     "image/gif",
"/assets/planets/ice.gif":       "image/gif",
"/assets/planets/gas_giant.gif": "image/gif",
```

Предзагрузка на клиенте:
```javascript
const ASSET_TYPES = ['lava', 'rocky', 'ice', 'gas_giant'];
const loadedAssets = {};
function preloadAssets() {
  return Promise.allSettled(ASSET_TYPES.map(type => new Promise((resolve, reject) => {
    const img = new Image();
    img.onload = () => { loadedAssets[type] = img; resolve(); };
    img.onerror = reject;
    img.src = `/assets/planets/${type}.gif`;
  })));
}
```

---

## 41. ОТВЕТ GET /system/{id}

```json
{
  "star_id": 42,
  "star_type": "yellow_dwarf",
  "mass": 420,
  "gen_code": "044221025",
  "n_planets": 8,
  "n_rings": 0,
  "planets": [
    { "slot": 3, "orbit_index": 2, "type": "rocky", "visual_type": "rocky", "period_sec": 14400 },
    { "slot": 7, "orbit_index": 6, "type": "ice",   "visual_type": "ice",   "period_sec": 86400 }
  ],
  "rings": [
    { "ring_index": 1, "orbit_grid": 14 }
  ]
}
```

Позиции вычисляются клиентом из `period_sec` + `gen_code` + `star_id` + `Date.now()`.

---

## 42. СИНХРОНИЗАЦИЯ С РЕЕСТРОМ

- При смерти звезды (`/registry/death`) → файл `systems/star_{id}.json` помечается как архивный (звезда мертва, постройки разрушены)
- При `POST /registry/reset` → удаляются все файлы `systems/`

---

## 43. КОНТРОЛЬНЫЙ СПИСОК РЕАЛИЗАЦИИ

### Этап 1 — MVP визуализации ✅

- [x] `GET /system/{id}` — ответ: star_type, mass, gen_code, planets[], rings[]
- [x] Двойной клик по звезде → `openSystemView(star)`
- [x] Оверлей с заголовком и canvas
- [x] `requestAnimationFrame` рендер-цикл
- [x] Рендер примитивов: звезда, орбитальные кольца, планеты с гало
- [x] Закрытие: кнопка + `Escape`
- [x] Панель среды: TEMP, RADIATION, METEOR — живые бары (обновление каждые 8 кадров)
- [x] Тултип галактической карты: RAD, MA, Rbirth, Rings для звёзд
- [x] Формулы RAD и MA (§16.1) реализованы в `computeRAD` / `computeMA`
- [x] MA: спайк от поглощения формации (`meteor_spike_0/tick`, 5 тиков, до +50)
- [x] MA: спайк от бомбардировки asteroid_field (`ast_spike_0/tick`, 3 тика, до +30)
- [x] Синхронизация времени планет с галактическим тиком (§39.7)
- [x] Детерминированные параметры планет — 6 значений [1–100] (§34.4), кеш в `p._params`
- [x] Визуальный радиус планеты пропорционален параметру RADIUS (4–14px)
- [x] Hover-панель планеты `#sys-planet-info` с 6 барами (нижний правый угол)
- [x] Курсор `pointer` при наведении на планету в system view
- [x] Орбитальный алгоритм: зазор ≥ 3 слота (2 свободные орбиты), 36 слотов, 12 типов
- [x] Диапазон орбит расширен: 6%–50% canvas (формула `0.06 + 0.44 × t`)
- [x] Алгоритм размещения колец: зазоры между планетами, топ-3 × LCG (§34.1b)
- [x] Formation View: двойной клик по формации, 2–6 колец, LCG-сид от form.id (§12)
- [x] Динамический T_norm для CORE_TEMP: `star.temp / 500` (живая температура звезды)
- [x] Каскадная трансформация планет: coreTemp > 100 → ice→rocky→lava→destroyed, gas_giant→rocky (§34.5)
- [x] Регистрация звёзд из remnant_decay в `disperseObject` (fix: белые карлики открывались с 404)
- [ ] `preloadAssets()` — предзагрузка GIF

### Этап 2 — Хранение игровых данных

- [ ] Директории `systems/` и `players/` создаются автоматически при старте
- [ ] `GET /system/{id}/state` — только файл изменений
- [ ] `POST /system/{id}/build` — запись постройки / станции
- [ ] `POST /system/{id}/fleet` — запись флота
- [ ] `DELETE /system/{id}/object/{obj_id}` — удаление объекта
- [ ] `GET /player/{player_id}` — индекс систем игрока

### Этап 3 — Ресурсы и экономика

- [ ] `baseResources(planetType, genCode)` — ресурсы по типу и коду
- [ ] Производство построек и потребление ресурсов
- [ ] Механика добычи, очереди

### Что НЕ входит в текущий модуль

- Типы построек и их производственные эффекты
- Механика боя флотов внутри системы
- Луны (орбиты вокруг планет)
- Кометы и метеориты внутри системы
- Терраформирование планет
